{
  "config": {
    "step": {
      "user": {
        "title": "Macau Bus",
        "data": {
          "device_tracker": "GPS Device",
          "routes": "Route filter (comma-separated, empty = all)",
          "radius": "Search Radius (m)",
          "scan_interval": "Update Interval (s)"
        },
        "description": "Select a GPS device to locate nearby bus stops and routes."
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Settings",
        "data": {
          "device_tracker": "GPS Device",
          "routes": "Route filter (comma-separated, empty = all)",
          "radius": "Search Radius (m)",
          "scan_interval": "Update Interval (s)"
        }
      }
    }
  }
}
